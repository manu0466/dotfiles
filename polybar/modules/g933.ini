[module/g933]
type = custom/script
interval = 1

exec = headsetcontrol -bc
exec-if = headsetcontrol -bc
format = <label>
format-prefix = "%{T5} %{T-}"
format-prefix-foreground = ${colors.icon}
label = %output%%
